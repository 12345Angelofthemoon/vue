关于数组遍历中改变删除本数组的某一项会发生什么情况呀
今天出了一个bug找了半天发现是这样

--------------------------------------------------------------------------------

1.双向绑定——v-model
2.虚拟DOM
3.生态圈

指令——增强功能
v-bind    :
v-on      @
v-for
v-model
v-if
v-show
v-html

v-pre v-once v-cloak

--------------------------------------------------------------------------------

1.原理
  渲染
    还行
  双向绑定
    <input v-model="aaa" />
    <input :value="aaa" @input="aaa=$event.target.value" />

2.computed-计算属性
  - 带有缓存：computed只在data变化时重新计算——提高性能
  - 可读可写：

  什么时候用？
  - 提高新能
  - 为了方便

3.watch
  - 不带缓存
  - 不可写


    computed               watch              方法
    强调的是数据            强调的是操作        函数
    数据是虚拟的

4.组件
5.vue-router
  Object.defineProperty

--------------------------------------------------------------------------------

watch是不是要开着一个线程一直盯着 那个数据

$set(vm.arr, 1, 'xxx');
$delete(vm.arr, 1);

--------------------------------------------------------------------------------

Vue组件：
1.声明
  - 全局
  Vue.component('xxx', {
    data(){
      return {};
    }
  });

  - 局部
  {
    data, methods,...,
    components: {
      xxx: {
        data(){},
      }
    }
  }

  - 类声明

2.调用

--------------------------------------------------------------------------------

组件：
1.声明组件
  全局：Vue.component('name', {...});
  局部：new Vue({
    components: {
      name: {...}
    }
  });

  类声明：
  let Cmp=Vue.extend({...});

  components: {
    ...
  }

2.创建
  <cmp a="xx" :b="xx" />
  let cmp=new Cmp({
    propsData
  });

3.动态组件
  <component is="" />

--------------------------------------------------------------------------------
